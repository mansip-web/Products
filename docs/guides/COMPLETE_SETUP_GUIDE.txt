╔══════════════════════════════════════════════════════════════════╗
║         🔧 FIX: "Failed to add to cart" Error                  ║
╚══════════════════════════════════════════════════════════════════╝

❌ PROBLEM: Cart and Wishlist tables don't exist in database

✅ SOLUTION: Run ONE SQL script to set up BOTH features

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 QUICK FIX (2 MINUTES):

   STEP 1: Open Supabase Dashboard
           🔗 https://app.supabase.com/project/qhgiqtowtgmreekgipud

   STEP 2: Click "SQL Editor" in left sidebar

   STEP 3: Click "New Query" button

   STEP 4: Open this file in your project:
           📄 COMPLETE_SETUP.sql

   STEP 5: Copy ALL the SQL code from that file

   STEP 6: Paste into Supabase SQL Editor

   STEP 7: Click "RUN" button

   STEP 8: Wait for success message:
           ✅ "Setup Complete! ✅"
           ✅ wishlist_created: 1
           ✅ cart_created: 1

   STEP 9: Go back to your app and REFRESH (F5)

   STEP 10: Test both features:
            • Click heart icon → Should add to wishlist ✅
            • Click "Add to Cart" → Should add to cart ✅

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 WHAT THIS SCRIPT DOES:

   ✓ Creates wishlist table
   ✓ Creates cart table
   ✓ Sets up security policies (RLS)
   ✓ Grants permissions
   ✓ Creates auto-update trigger for cart
   ✓ Verifies everything is set up correctly

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ AFTER RUNNING SQL:

   1. Refresh your browser (Ctrl + Shift + R)
   2. Try adding to wishlist → Should work ✅
   3. Try adding to cart → Should work ✅
   4. Check "My Wishlist" tab → See items ✅
   5. Check "My Cart" tab → See items ✅

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🆘 STILL NOT WORKING?

   After running SQL, if still getting errors:

   1. HARD REFRESH: Ctrl + Shift + R (or Cmd + Shift + R)
   2. Clear browser cache
   3. Logout and login again
   4. Check browser console (F12) for errors
   5. Verify tables exist:
      
      Run in SQL Editor:
      SELECT * FROM wishlist;
      SELECT * FROM cart;
      
      Both should show empty tables (no error)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📁 FILES AVAILABLE:

   ✓ COMPLETE_SETUP.sql       - ONE script for BOTH features
   ✓ WISHLIST_MIGRATION.sql   - Wishlist only
   ✓ CART_MIGRATION.sql       - Cart only
   ✓ WISHLIST_ERROR_FIX.txt   - Wishlist troubleshooting
   ✓ CART_SETUP.txt           - Cart setup guide

   👉 USE COMPLETE_SETUP.sql - It's the easiest!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏱️ TIME TO FIX: 2 minutes
🎯 DIFFICULTY: Easy (just copy & paste SQL)

╚══════════════════════════════════════════════════════════════════╝
